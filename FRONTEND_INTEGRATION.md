# 🔗 Интеграция фронтенда с AI агентом

## ✅ Что уже настроено

- ✅ Агент слушает на `http://127.0.0.1:9000`
- ✅ Фронтенд настроен на подключение к агенту
- ✅ Все endpoints совпадают

## 🚀 Запуск фронтенда

### 1. Установить зависимости
```bash
cd frontend/my-react-flow-app
npm install
```

### 2. Запустить сервер фронтенда
```bash
npm run dev
```

Фронтенд откроется на `http://localhost:5173` (или другом доступном порту)

### 3. Убедиться что агент запущен
В другом терминале:
```bash
cd agent
python run_server.py
```

Агент должен быть на `http://127.0.0.1:9000`

## 🧪 Проверка работы

1. Откройте браузер: `http://localhost:5173`
2. Найдите кнопку Chat (или откройте ChatWindow)
3. Напишите сообщение
4. Должны получить ответ от AI агента

## 📡 Как работает интеграция

### Endpoints используемые фронтенд:

1. **POST /api/v1/agent-chat** - обычный запрос (одиночный ответ)
   ```javascript
   const response = await fetch('http://localhost:9000/api/v1/agent-chat', {
     method: 'POST',
     headers: {'Content-Type': 'application/json'},
     body: JSON.stringify({message: 'Hello!'})
   });
   const data = await response.json();
   console.log(data.answer); // Ответ от AI
   ```

2. **POST /api/v1/agent-chat/stream** - потоковый ответ (реальное время)
   ```javascript
   const response = await fetch('http://localhost:9000/api/v1/agent-chat/stream', {
     method: 'POST',
     headers: {'Content-Type': 'application/json'},
     body: JSON.stringify({message: 'Hello!'})
   });
   
   const reader = response.body.getReader();
   while (true) {
     const {value, done} = await reader.read();
     if (done) break;
     // Обработать потоковые данные
   }
   ```

3. **POST /api/v1/adk/chat** - ADK версия
4. **POST /api/v1/adk/chat/stream** - ADK потоковая версия

## ⚙️ Конфигурация

### Локальная разработка (.env.local)
```env
VITE_API_URL=http://localhost:9000
```

### Production (.env или переменная окружения)
```env
VITE_API_URL=https://your-agent-domain.com
```

## 🐛 Отладка

### Проблема: "Failed to fetch from agent"

**Решение 1:** Проверьте что агент запущен
```bash
curl http://127.0.0.1:9000/api/v1/health
# Должен вернуть: {"ok": true}
```

**Решение 2:** Проверьте что CORS включена
```bash
# Агент уже имеет CORS настройку, но проверьте логи
```

**Решение 3:** Проверьте что используется правильный URL
```bash
# В браузере консоль (F12) -> Network
# Проверьте что запросы идут на http://localhost:9000
```

### Проблема: "Mock agent response"

Это значит что агент в mock режиме. Нужно:
1. Убедиться что LLM_API_KEY установлен в .env агента
2. Перезагрузить агент
3. Проверить логи при запуске

### Проблема: Timeout при отправке сообщения

**Решение:** Увеличить timeout в коде (если нужно)
```typescript
// В api.ts можно добавить timeout
const timeout = new Promise((_, reject) =>
  setTimeout(() => reject(new Error('Timeout')), 30000)
);
Promise.race([fetchPromise, timeout])
```

## 📊 Тестовые запросы через фронтенд

1. **Простой вопрос:**
   ```
   "What is Python?"
   ```

2. **Вопрос требующий размышления:**
   ```
   "Explain the concept of machine learning in simple terms"
   ```

3. **Русский язык:**
   ```
   "Что такое искусственный интеллект?"
   ```

4. **С историей (context):**
   Напишите несколько сообщений подряд - фронтенд будет отправлять историю

## 🎯 Потоковый режим (Streaming)

Фронтенд поддерживает потоковый ответ для лучшего пользовательского опыта.

Включить streaming:
- В ChatWindow найдите переменную `streamEnabled`
- Установите в `true`

Потоковый режим будет показывать ответ по частям по мере его генерации.

## 📚 Полезные ссылки

- **Агент:** [ENABLE_AI.md](../../agent/ENABLE_AI.md) - как настроить реальный LLM
- **Фронтенд:** `frontend/my-react-flow-app/README.md`

## ✨ Все готово!

Фронтенд полностью интегрирован с агентом. Просто:

1. ✅ Запустите агент: `python run_server.py` (в папке agent)
2. ✅ Запустите фронтенд: `npm run dev` (в папке frontend/my-react-flow-app)
3. ✅ Откройте чат и пишите сообщения!

**Агент должен отвечать на все вопросы в реальном времени!** 🚀
